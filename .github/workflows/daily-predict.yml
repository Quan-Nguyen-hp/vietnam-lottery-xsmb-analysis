name: Daily Predict & Update (DISABLED)

# Đã vô hiệu hóa theo yêu cầu người dùng
on:
  workflow_dispatch:

jobs:
  predict-and-update:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: Disabled
        run: echo "Workflow disabled"
