name: Update data (DISABLED)

# Đã vô hiệu hóa theo yêu cầu người dùng
on:
  workflow_dispatch:

jobs:
  update-data:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: Disabled
        run: echo "Workflow disabled"
